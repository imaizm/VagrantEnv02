Listen {{ pico_port }}

<VirtualHost *:{{ pico_port }}>
	DocumentRoot {{ pico_install_path }}

	<Directory "{{ pico_install_path }}">
		AllowOverride all

		# < apache 2.4
	#	Order allow,deny
	#	Allow from all
		# >= apache 2.4
		Require all granted
	</Directory>
</VirtualHost>
